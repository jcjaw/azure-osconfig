name: Test

on: [push]

jobs:
  test:
    name: Unit test
    runs-on: ubuntu-latest

    steps:
     - uses: actions/checkout@v3

     - name: test action
       uses: ./.github/actions/check-logs
       with:
        logs: |
          blah.log
          123.log